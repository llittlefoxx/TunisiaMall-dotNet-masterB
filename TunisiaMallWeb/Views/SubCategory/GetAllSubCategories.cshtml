@model IEnumerable<TunisiaMall.Domain.Entities.subcategory>

@{
    ViewBag.Title = "GetAllSubCategories";
}

<h2>Tunisia Mall SubCategories</h2>

<br />
<table class="table">
    <tr>
        <th>
            @Html.DisplayName("SubCategory Libelle")
        </th>

        <th>
            @Html.DisplayName("Description")
        </th>
      
        <th>
            @Html.DisplayName("Category")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
       
        <td>
            @Html.DisplayFor(modelItem => item.libelle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.description)
        </td>
      
        <td>
            @Html.DisplayFor(modelItem => item.category.libelle)
        </td>
        <td>
           
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
           
        </td>
    </tr>
}

</table>
