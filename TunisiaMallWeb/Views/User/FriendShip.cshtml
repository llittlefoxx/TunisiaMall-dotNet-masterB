@using TunisiaMall.Domain.Entities

@model IEnumerable<TunisiaMall.Domain.Entities.user>

@{
    ViewBag.Title = "FriendShip";
    
}

<h2>FriendShip</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        
        <th>
            @Html.DisplayNameFor(model => model.firstName)
        </th>
        
        <th>
            @Html.DisplayNameFor(model => model.lastName)
        </th>
       
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        
        <td>
            @Html.DisplayFor(modelItem => item.firstName)
        </td>
        
        <td>
            @Html.DisplayFor(modelItem => item.lastName)
        </td>
       
        
        <td>
     
         <button type="button" value="AddFriend" onclick="@Url.Action("addFriend","User",new {idUser2=item.idUser }) ">AddFriend</button>
                    

        </td>
    </tr>
}

</table>
<div>
   
    @Html.ActionLink("GoToProfile", "GetProfile")
</div>
